{
  "name": "encurtador-de-links",
  "version": "1.0.0",
  "description": "",
  "main": "build/out.js",
  "scripts": {
    "start": "node build/out.js",
    "build": "esbuild src/index.ts --platform=node --bundle --outfile=build/out.js",
    "watch": "esbuild src/index.ts --platform=node --bundle --watch --outfile=build/out.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "esbuild": "^0.14.36",
    "express": "^4.17.3"
  }
}
