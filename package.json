{
  "name": "encurtador-de-links",
  "version": "1.0.0",
  "description": "",
  "main": "build/out.js",
  "scripts": {
    "start": "node build/out.js",
    "build": "esbuild src/index.ts --bundle --outfile=build/out.js",
    "watch": "esbuild src/index.ts --bundle --watch --outfile=build/out.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.36"
  }
}
